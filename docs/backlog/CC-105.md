---
id: CC-105
title: CI security-audit gate red on advisory-DB drift — next (critical RCE), sharp, js-yaml, hono
status: done
priority: P0
area: infra
epic: public-launch
created: 2026-09-09
updated: 2026-09-09
---

# CC-105 — Security audit gate: advisory drift remediation

## Problem

The `Security audit` step (`npm audit --audit-level=high`) in `ci.yml` began failing
2026-09-09 on **unchanged dependency state**: master's last green run was 2026-09-08
02:17Z (`64659da`), and the first run after new advisories landed in the registry
(34303693889, on CC-104's PR — which itself touched zero package files) failed with
10 vulnerabilities (7 moderate, 2 high, 1 critical). Classic advisory-DB drift: the
codebase did not change; the world's assessment of it did.

Gate-blocking findings (`--audit-level=high` means moderates do not block):

| Package | Path | Severity | Current | Patched | Published | Quarantine (7d) |
| :-- | :-- | :-- | :-- | :-- | :-- | :-- |
| `next` | direct | **critical** — GHSA-p293-qw3h-jr36 (unauth RCE, Windows hosts), GHSA-2xp9-vwfh-vxw4 (Image Optimization AVIF RCE) | 16.2.12 | 16.3.4 | 2026-08-31 (9d) | ✅ clears |
| `sharp` | via `next` | high — libheif GHSA-rgj7-g3m4-5g8c | 0.35.3 | 0.35.4 | 2026-08-26 (14d) | ✅ clears |
| `js-yaml` | transitive | high — GHSA-2883-xcg3-v3hh | 4.3.1 | 4.3.2 | 2026-08-26 (14d) | ✅ clears |
| `hono` | transitive (`@modelcontextprotocol/sdk`, `@hono/node-server`, `porto`) | moderate ×3 — GHSA-gqvv-2mrq-wpjv, GHSA-g6gw-c38x-mqfc, GHSA-crvj-82cr-hjcx | 4.13.0 | 4.13.5 | 2026-08-26 (14d) | ✅ clears |

**Quarantine note (PO directive 2026-08-30):** `hono@4.13.6` and `4.13.7` (both
2026-09-04, 4d old) are INSIDE the 7-day window and must NOT be installed; the target is
**4.13.5** exactly. All other targets clear the gate. No PO exception required.

Not in scope: the moderates that remain after this fix (if any) do not gate CI.

## Changes

- `next` 16.2.12 → 16.3.4 (direct; clears the critical)
- `sharp` 0.35.3 → 0.35.4, `js-yaml` 4.3.1 → 4.3.2, `hono` 4.13.0 → 4.13.5 (lockfile;
  pinned via `overrides` where the resolver would otherwise float past 4.13.5)

## Acceptance

- [x] Quarantine matrix verified against registry publish timestamps (table above,
      raw registry queries, 2026-09-09)
- [x] Lockfile post-install asserts exact target versions (incl. hono ≠ 4.13.6/7)
- [x] `npm audit --audit-level=high` exits 0
- [x] `npm test`, `npm run lint`, `npm run typecheck` green
- [x] PR merged (#202); master CI green post-merge (run 34304561182)

## Still open

- None remaining in this ticket. The `build`-as-required-status-check defect spun out
  to `CC-106` (PO action; found when PR #201 auto-merged with the audit step red).

## Related

`CC-104` (the PR that surfaced this) · supply-chain quarantine directive 2026-08-30 ·
`Lessons-Learned` dependabot-lag note.
